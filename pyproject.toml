[project]
name = "pointcloud-preprocessor"
version = "0.1.0"
description = "Simple point cloud preprocessing tool"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"